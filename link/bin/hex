#!/bin/python

import sys


def bits_set(n: int):
    return [i]


if len(sys.argv) < 2:
    sys.exit(1)

n = int(sys.argv[1], 0)
print(f"{n}")
print(f"0x{n:x}")
print(f"0b{n:b}")
for i in range(n.bit_length()):
    if (n >> i) & 1:
        print(f"  {i}")
