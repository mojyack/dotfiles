#!/bin/sh

if [[ ! "$#" -eq 0 ]]; then
    type tmux >/dev/null 2>&1
    exit 
elif [[ -z $TMUX ]]; then
    tmux has-session && tmux list-sessions >/dev/null
    if [[ ! $? -eq 0 ]]; then
        tmux new -d -s $(whoami) &
    fi
    tmux a -t $(whoami)
fi
